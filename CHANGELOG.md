# 变更记录

本文件记录用户可见的已发布变化；开发过程和当前待办不在此累积。

## 1.5.1

- 桌面外壳和左侧操作栏迁入常驻 React + TypeScript DOM，删除首页模块；所有业务入口按文件处理、数据同步和自动化任务直接导航，原生模块仅覆盖右侧内容区。
- 冷启动改为中性加载层，避免先显示旧导航和旧模块文案；复用单一 WebView2 宿主，并锁定页面缩放。
- 正式前端内置 Inter Variable 与 Noto Sans SC Variable，静态文字只允许 400、500、600、700 标准字重；新增字体字重检查，修复侧栏小字号插值字重造成的模糊感。
- “生产消息 Notion 入库”按已确认 Demo 迁入正式 React 页面，同时保留真实解析、Notion 检查、逐字段冲突决策和写入复查链路；移除示例解析代码与示例结果。
- 生产消息每次解析按消息类型读取最新 Notion Schema，并以目标库字段映射驱动核对；映射字段缺值或类型异常时阻止写入，批量消息不再静默过滤无效记录。
- 修复塔筒日报“产出（套）”被误匹配为节数字段的问题；日报只写当日数据，当月和全年累计不回填日报或触发新增月、年记录。
- 核对状态统一为新增、一致、待确认、异常：新增和一致使用安静的中性字段胶囊，一致的顶部提示改为灰色，待确认使用黄色，异常使用红色并统一字体渲染。
- 冲突选择区集中放在数据表下方并同时展示原值和新值；移除鼠标选值后的矩形焦点框，保留键盘可辨识焦点。
- 生产消息模块标题固定在右侧内容区顶部，滚动条从标题下方开始；增加与输入框一致的 squircle“模板配置”占位按钮，为后续可视化消息模板映射预留入口。
- 目标 Notion Schema 与消息解析改为独立准备：右侧字段始终以目标库映射为准，消息缺值不会导致字段在检查期间消失；手工编辑只更新本地值，提交时统一由服务端最终复查。
- 找到同日期记录且所有字段一致时改为真正的无写入结果，不再查询月度/年度关联或调用 Notion 页面更新；批次结果明确显示“无需写入”。
- 报表中心与生产消息统一到同一字号体系：页面标题 26px、正文和区域标题 16px、控件和说明 15px、标签、状态及辅助信息 14px；清除旧 13px 和旧绿色设计例外，继续使用 Inter Variable + Noto Sans SC Variable 与标准字重。

## 1.5.0

- 修复 Windows 下 Node Playwright 与 C# 之间未固定 UTF-8，导致中文进度和返回文件路径乱码、任务停在导出完成而无法进入解析汇总的问题；标准输入改为无 BOM UTF-8，runner 同时兼容 BOM，避免启动时 JSON 第一字符解析失败。
- 报表中心新增真实任务进度：后台逐日查询、导出、解析及生成汇总时，由 C#/Playwright 主链路持续回传当前操作和完成数量。
- 报表中心 Playwright 改为完全后台运行，不再创建 `.downloads` 临时目录；修复 FineReport 标题区重复值导致 ClosedXML 在找到正式表头前中止、汇总无法执行的问题。
- 报表中心使用 `https://fr.tz.com.cn:8443/webroot/decision` 作为报表入口，并恢复单次启动 Chromium、单次进入加工日报、循环导出全部日期。
- 测试阶段已有原始日报始终重新下载覆盖；单日报失败最多重试三次并继续后续日期，存在失败日期时不生成汇总。
- 新增正式“报表中心”React/WebView2 路由，首个业务为机加工实开台时周期汇总。
- 迁移 Playwright 采集与 ClosedXML 解析 PoC：支持本地登录状态、21 日至次月 20 日周期、逐日归档、动态表头、设备完整性和汇总 Excel。
- 新增 YAML 业务配置、结构化运行摘要及 Core/Infrastructure 回归测试；明文 FineReport 凭据不进入正式工程。
- 修复报表中心“输出位置”卡片被全局侧栏样式固定定位并遮挡操作区的问题。
- 报表中心改为手动选择开始、结束日期并由结束日期自动确定汇总月份；源目录、输出目录、报表网页和 DPAPI 加密账号密码集中在独立配置区。
- 修复 FineReport 查询完成后导出菜单无法继续的问题：恢复已验证的嵌套 `div` 菜单检测、Excel hover 展开、分页导出可见检查及提前下载监听顺序。
- 报表中心增加分区线与清晰输入边界，日期范围改用应用内统一日历控件。
- React 工作区冷启动时先显示与当前路由标题一致的原生加载状态，WebView2 与 React 就绪后再切换内容；三个 React 路由复用同一宿主，并增加启动阶段计时、超时错误和重试入口，避免初始化期间暴露整页白屏。
- 修复 React 页面已经正常显示后仍被 15 秒超时错误覆盖的问题；就绪计时器现在会在导航前建立，并且只有当前导航能够触发错误状态。
- 修复首页“打开生产消息”和生产消息模块卡片被消息桥导航白名单拒绝、点击后无响应的问题。
- 生产消息核对页不再显示误导性的“可写入”或允许手工改选目标数据库：解析通过后统一为“待检查”，写入位置由消息类型和绑定自动确定。“写入位置”标题与“业务日期”一致放在控件外上方，框内只显示数据库图标和名称。预览和写入只保留目标数据库已映射字段；重新保存数据源后，新增的可写属性可用“属性名：值”无代码扩展解析。
- 日报任务详情新增“发送今日消息”，使用已验证的正式模板发送并按日期和模板版本防止重复推送。
- Release 启用日报任务时，如果当天已过计划时间且没有成功发送记录，会立即补发；任务计划始终绑定当前 Release 可执行文件。
- Debug 不再共享显示 Release 的任务开启状态，也不能安装、停用或修改 Windows 定时任务；只有 Release 支持定时发送。

## 1.4.7

- 生产消息入库重构为与日报配置一致的四步切屏工作台，批量核对采用消息队列与当前详情布局，默认日期固定在解析工具行，核对区只显示实际写入的非空业务字段，数据源绑定移至页头设置；日报和生产消息直接复用同一个进度条组件、节点与轨道动画。

- 日报任务详情改为四步渐进式配置：基本信息、钉钉机器人、编辑消息、预览与测试；步骤切换使用分段进度线和非线性节点动画，前进与回退严格按“节点、进度线、页面”顺序执行。
- 重做消息编辑与预览工作区比例，数据源字段按业务数据页、具体数据库、数据字段三级选择；编辑区和预览区限制在一致高度并在内部滚动，避免内容溢出和页面底部大面积空白。
- 保存机器人配置时只执行不带消息体的网络连通检查，不向钉钉群发送消息；Webhook 与 Secret 在最终测试发送时真实验证，测试成功后仍需返回任务列表手动启用。
- 任务列表删除操作移入右键菜单并保留二次确认；移除重复的进入编辑箭头，新建任务与启停操作不再互相锁定。
- 统一首页、生产消息和日报推送三个 React 页面中的正文、表单、辅助说明、状态标签、弹窗文字和清新低饱和绿色主题，并修复日报时间选择器打开时未定位当前小时和分钟的问题。

## 1.4.6

- 日报推送完整迁移到 React/WebView2 新界面，任务列表集中管理启停和删除，配置页按基本信息、机器人、模板/数据源、预览测试和运行记录顺序组织。
- 删除“保存模板”“发布为任务版本”和“应用定时设置”等重复动作；配置自动保存，测试成功后进入可启用状态，发送相关修改会自动停用并要求重新测试。
- 日报数据页、数据库、字段、发送时间和预览日期改为与现有 React 页面一致的自绘选择控件；预览日期只复用生产消息日期选择器的界面，不共享业务状态或 Notion 同步逻辑。
- 修复日报路由错误回退到主页的问题；钉钉发送严格使用模板生成内容，不再自动附加 `@所有人`。
- 已知细节：部分日报控件字体大小及时间下拉默认显示仍需继续调整。

## 1.4.5

- 首页和“生产消息 Notion 入库”正式切换至 React + TypeScript + Motion 新版界面，复用 WinUI 壳的唯一侧栏，不再嵌套前端导航；重复的“新版界面预览”入口已移除，原生页面代码继续保留用于回退。
- 新版生产消息页将操作明确拆分为“解析、核对、检查数据库、写入”；解析不再联网，编辑后旧检查立即失效，写入时继续复用后端复查、覆盖确认和月计划规则。
- 修复新版界面中解析字段经过 React/C# 消息桥往返后被写入前校验清空的问题；解析成功的有效记录现在可继续检查和写入。
- 新版日期选择器增加日期、12 个月和附近 12 年三级选择；生产消息数据源区按下料、塔筒两类完整显示日报、月累计和年累计去向，并移除无独立作用的“刷新状态”。
- 操作反馈改为跟随解析、检查和写入区域就近展示；检查进度不再占用解析按钮，可写入、警告和冲突使用明确的语义色。
- 本地验证新增 React 消息桥测试、TypeScript 检查和离线生产 bundle 构建；发布物继续保持单一 Windows x64 自包含入口。

- 日报任务详情将消息模板与数据源面板固定为 7:3 左右布局，标题和高度对齐，编辑框右上角使用单一开关切换；字段按完整数据页、原始数据库和数据字段逐级选择，业务日期胶囊移入数据源面板，并移除显示格式与绑定详情。
- 日报任务名称与发送时间改为左右布局，测试发送和任务启停集中到任务概览；钉钉推送配置改为设置页风格的常驻输入行，已保存凭据仍不回填。
- 日报定时设置合并到任务概览，推送凭据改为安全摘要与空白编辑态，运行记录默认展示最近 5 条并支持查看最多 100 条。
- 应用最小窗口调整为 1100×700；本地验证默认不覆盖正式 `publish\Release`，已安装计划更新时保留有效的原程序路径。

## 1.4.3

- 日报任务列表和任务详情改为占满主内容区，最大化窗口时不再受固定最大宽度限制。
- 校正任务列表的默认内边距和卡片阴影占位，使卡片与页面标题、状态提示条边缘对齐。
- 本地发布固定输出 `publish\Debug` 测试版和 `publish\Release` 正式版，不再生成 ZIP 或 artifacts 产物。

## 1.4.2

- 侧边栏取消业务模块的“使用页 / 配置页”二级入口，每个模块改为一个可直接打开的一级入口；每日焊接和生产消息在各自页面连续显示操作区与配置区。
- 将内置模块归纳为文件处理、数据同步和自动化任务三类，并同步首页与侧边栏分组。
- 挂网计划 PDF 与生产会资料拆分复用统一文件流程外壳；生产会改为选择文件后手动开始拆分。
- 将日报自动推送归入自动化任务分类，继续使用现有日报多任务、定时计划和运行记录，不建立跨业务通用执行引擎。

## 1.4.1

- 日报自动推送改为任务列表与单任务详情，每个任务独立管理模板、Notion 字段、钉钉凭据和发送时间。
- 支持多个日报任务独立定时运行，并增加草稿/测试/发布流程及最近 100 条结构化运行记录。
- 支持查看单次运行的来源、业务日期、模板版本、执行阶段、重试次数和脱敏结果。
- 自动迁移旧版单一日报配置；迁移后重新启用即可安装按任务 ID 区分的新任务计划。
- 钉钉凭据改为任务独立配置，并在输入框中直接显示脱敏占位内容。
- 启用任务需先发布有效模板并配置钉钉；停用后才能删除任务及其记录。
- 任务列表增加正常与异常数量摘要，仅已启用任务计为正常；草稿和已停用任务计为异常。
- 修复从未安装 Windows 任务计划的新任务无法删除的问题，并将操作结果调整为分区就近提示。
- 修复任务卡片悬停背景不完整和颜色影响可读性的问题，保留完整圆角背景、按下反馈和轻量外扩阴影。
- 修复模板编辑器遗留回车符与段落换行混用、导致预览中行粘连或空行不一致的问题。

## 1.4.0

- 新增日报模板编辑、自动推送、Notion 数据源绑定和定时任务配置。
- 改进首页工具分类、离线配置就绪状态和共享导航。

## 1.3.0

- 新增生产消息解析、Notion 写入检查和覆盖写入流程。

## 1.2.0

- 完善挂网计划 PDF 检查、修复、复查和导出流程。

## 1.1.0

- 增加生产会资料拆分及发布加固。

## 1.0.0

- 首个生产助手版本。
