# 生产助手 PRD

> 本文档回答“为什么做、做什么、做到什么程度算完成”。开发接手、构建、验证和 Git 信息见 [HANDOFF.md](HANDOFF.md)。

| 项目 | 内容 |
| --- | --- |
| 产品 | 生产助手 |
| 当前版本 | 1.4.0 |
| 当前 Release | 公开仓库尚未发布正式版本 |
| 文档状态 | 当前代码实现基线 |
| 文档日期 | 2026-08-10 |
| 桌面入口 | `src/ProductionAssistant.App` |
| 业务核心 | `src/ProductionAssistant.Core` |
| 外部适配 | `src/ProductionAssistant.Infrastructure` |

## 1. 文档边界

PRD 记录产品目标、用户、功能范围、业务规则、验收标准和后续规划。

- “当前实现”：代码中已经存在，并能通过代码或构建确认的能力。
- “需样本验证”：代码已有，但必须用真实 Notion 数据或 Excel 模板确认。
- “待开发”：当前代码没有实现，不写成已完成。
- 具体文件路径、构建命令、日志位置、Git 故障和接手步骤统一放在 `HANDOFF.md`。

## 2. 产品定义

### 2.1 产品定位

生产助手面向生产现场、生产计划和生产会资料整理场景，把重复的生产数据计算、计划表审查、PDF 候选导出和 Excel 分段整理集中到一个 Windows 桌面应用中。

### 2.2 产品目标

- 减少每日生产数据的手工计算和录入。
- 在导出或拆分 Excel 前发现结构、序号、日期和公式问题。
- 保护源 Excel 和正式文件，优先生成可检查的副本或候选文件。
- 为后续生产工具提供统一的入口、状态提示和验收口径。

### 2.3 目标用户

- 生产计划和现场数据维护人员。
- 整理挂网计划、生产会资料和候选 PDF 的计划人员。
- 需要把生产数据同步到 Notion 的业务人员。

## 3. 产品范围

| 模块 | 入口 | 状态 | 主要结果 |
| --- | --- | --- | --- |
| 首页与导航 | `home` | 当前实现 | 本地工具、Notion 工具入口和本地配置就绪状态 |
| Notion 连接设置 | 系统设置 | 当前实现 | 连接配置、数据源缓存、字段绑定 |
| 每日焊接数据模拟 | `daily-weld` | 当前实现；Notion 需样本验证 | 每日数据、月/周/日层级同步 |
| 挂网计划 PDF 导出 | `plan-pdf` | 当前实现；Excel 需样本验证 | 审查结果、序号备份、候选 PDF |
| 生产会资料拆分 | `production-meeting` | 当前实现；Excel 需样本验证 | 三个分段 Sheet 的 `.xlsx` 副本 |
| 生产消息 Notion 入库 | `production-message` | 当前实现；真实写入待人工验收 | 解析下料和塔筒产线消息，写入日报库并关联每月/每年累计库 |
| 日报自动推送 | `daily-report` | 当前实现；真实链路待人工验收 | 组合日/月/年 Notion 数据，按自定义时间推送到钉钉群 |

### 3.1 当前用户主流程

1. 用户从首页进入所需工具。
2. 使用 Notion 模块前，先在系统设置中保存连接并刷新数据源。
3. 每日焊接模块生成并核对数据后，再导入 Notion。
4. 挂网计划模块先审查，再生成候选 PDF，人工检查后进入后续正式流程。
5. 生产会模块选择源 Excel，生成新文件，源文件不变。
6. 生产消息入库模块粘贴一条或多段带日期消息，预览、修正后写入 Notion；月度/年度汇总和比较由 Notion 完成。
7. 日报推送模块配置日/月/年来源和模板，预览、测试发送并启用后，由 Windows 任务计划自动执行。

## 4. 统一产品要求

### 4.1 页面和状态

- 应用启动后默认进入首页。
- 左侧导航以业务模块作为一级菜单，每个模块独占一行并可展开；模块下按真实能力显示“使用页”“配置页”“模板编辑”“推送配置”“运行记录”等二级页面。系统设置保留在底部用于全局 Notion 连接。
- 耗时操作显示进度或忙碌状态，并避免重复点击。
- 成功、警告、错误和不可执行状态必须有明确的中文说明。

### 4.2 文件安全

- 检查以只读方式打开源 Excel；修复功能先备份源 Excel，再将序号、行高、打印区域、分页和边框等规则写回并保存。
- 需要修复序号或格式时，必须先备份，再只修改允许的序号和打印属性。
- 生产会资料拆分必须输出新文件，源文件保持不变。
- 导出只读取修复并复查通过的源 Excel，不备份、不修改、不保存；导出失败只清理本次半成品 PDF，候选 PDF 输出到预览目录，不自动替换正式版文件。

### 4.3 依赖声明

- 目标环境为 Windows 10 1809 或更高版本，Windows 11 推荐。
- 目标架构为 x64，运行时为 .NET 8 和 WinUI 3。
- Excel 审查、PDF 导出和生产会资料拆分需要 Microsoft Excel 桌面版。
- Notion 功能需要已授权的 Integration、可访问的数据源和网络连接。

### 4.4 安全要求

- Notion 令牌不得以明文展示为本地持久化结果。
- 令牌只能由保存它的同一 Windows 用户使用。
- 应用错误和导入恢复信息不得泄露令牌、完整请求体、数据源 ID 或单元格内容。

## 5. 功能需求

### 5.1 首页与统一导航

#### 目标

让用户从一个入口按依赖类型看到当前工具、本地 Notion 配置状态，并快速打开需要的模块。

#### 功能要求

- 显示产品名称、简短说明和只代表当前设备的 Notion 本地配置总览。
- “本地工具”展示挂网计划 PDF 导出和生产会资料拆分；“Notion 工具”展示每日焊接数据模拟、生产消息 Notion 入库和日报自动推送。
- 首页继续按依赖类型展示“本地工具 / Notion 工具”；左侧不重复该分类，而是以五个业务模块为一级菜单、功能页为二级菜单。
- Notion 工具卡片按各模块现有绑定规则显示“本地绑定已就绪”或“待完成本地绑定”，首页不自动联网检查。
- 五张卡片整卡可点击，默认打开对应模块的使用页或模板编辑页，并与左侧导航同步展开模块和选中功能子页；总览提供系统设置入口。
- 当前子页为：挂网计划“使用页”；生产会资料拆分“使用页”；每日焊接“使用页 / 配置页”；生产消息“使用页 / 数据库配置”；日报自动推送“模板编辑 / 推送配置 / 运行记录”。没有持久化历史数据的模块不显示空记录页。
- 页面在合理的最小窗口尺寸内保持可用，内容不足时允许整体纵向滚动。

#### 验收标准

- 启动应用后默认显示首页。
- 五个业务模块均可从首页卡片和左侧导航打开。
- 未配置、数据源待刷新和本地配置已准备三种总览状态显示正确；返回首页后重新读取本地配置。
- 导航切换不会重复创建已经缓存的页面。

### 5.2 Notion 连接设置

#### 目标

集中保存 Notion 连接，让业务模块复用数据源缓存和字段绑定。

#### 输入

- Notion API 令牌。
- 可选的根页面 ID。

#### 功能要求

- 根页面为空时，按令牌权限发现数据源；填写根页面时，在根页面范围内递归发现数据源。
- 保存连接时刷新数据源缓存；连接未变化时允许复用缓存。
- 展示数据源列表、缓存数量和更新时间。
- 为不同业务模块保存数据源和字段映射。
- 令牌使用当前 Windows 用户的 DPAPI 加密保存，并保留上一份配置备份。

#### 验收标准

- 未填写令牌时，页面提示先配置连接。
- 保存有效连接后能够显示数据源缓存数量和更新时间。
- 修改令牌或根页面 ID 后，旧缓存被清除并重新获取。
- 重启应用后，当前 Windows 用户可以读取自己的连接配置。
- 配置文件内容不直接暴露明文令牌。

#### 限制

- 数据源发现结果受 Integration 授权范围限制。
- 连接、缓存和绑定属于本机当前 Windows 用户，不是团队共享配置。

### 5.3 每日焊接数据模拟

#### 目标

根据月度焊接总量生成按自然月份展开的每日数据，支持人工调整并同步到 Notion。

#### 输入

- 月度焊接总数量。
- 生产年份和月份。
- 已绑定的每日焊接 Notion 数据源。

#### 功能要求

- 根据年份和月份计算当月天数，支持闰年二月。
- 生成每天一行数据，包含日期、星期、数量和必要的低产说明。
- 生成结果包含周末减产和自然波动，所有日期数量合计必须等于月度计划。
- 用户可以修改单日数量；数量按非负整数处理。
- 用户确认人工修改后，固定这些日期并按原有权重重新分配其余日期。
- 实时显示计划合计、当前合计和差额。
- 月度合计不一致、仍有待重新分配或未绑定 Notion 时，禁止自动导入。
- 导入前检查目标自然月是否已有产量；已有数据时必须明确确认覆盖。
- 按月、周、日三层同步：
  - 月层默认寻找“每月焊接量”，需要月份、产量和日期变量字段。
  - 周层默认寻找“上周焊接量”，需要周期和日期范围字段。
  - 日层使用绑定的数据源，需要标题、日期、数量字段，以及指向月层和周层的 Relation。
- 已有唯一月、周或日记录时更新；缺失记录时创建；发现重复业务键时停止并提示。
- 导入过程显示当前阶段和每日进度。

#### 验收标准

- 有效输入生成的行数等于当月天数。
- 所有数量为非负整数，合计严格等于月度总量。
- 人工修改并重新分配后，固定日期不变，合计仍等于月度总量。
- 合计不一致时，自动导入按钮不可用。
- 目标月份已有数据时，未确认覆盖不能写入。
- 符合字段和 Relation 要求的三层 Notion 数据源能够更新或创建月、周、日记录。
- 导入异常时提示用户先检查目标数据再重试。

#### 限制与待开发

- 当前没有独立的 CSV 或 Excel 导出，主要输出路径是 Notion 自动导入。
- 月、周、日写入不是服务端事务，没有自动回滚。
- 三层数据源名称和字段结构存在约定，模板变化需要重新适配。

### 5.4 挂网计划审查与候选 PDF 导出

#### 目标

读取月份目录中的“一二三级计划” Excel，先发现问题，再生成不覆盖正式版的候选 PDF。

#### 输入要求

- 目录中且仅有一个文件名包含“一二三级计划”的 `.xlsx`、`.xlsm` 或 `.xls` 文件。
- 忽略文件名以 `~$` 开头的 Excel 临时文件。
- 目录中存在“正式版”文件夹。
- 当前目录或父目录名称能识别年份和月份，支持 `2026年\8月挂网计划` 和 `2026年8月挂网计划`。

#### 审查要求

- 以只读方式打开源 Excel。
- 检查可见 Sheet 和导出所需 Sheet 是否存在。
- 检查目录年月与表格标题年月是否一致。
- 检查普通序号从 1 开始、连续、无重复和无跳号。
- 检查项目计划的顶层项目号、子序号和归属关系。
- 检查计划完成、实际或预计完成、交付、发运等日期异常。
- 检查项目计划的完成时间、完成状态和实际完成时间三列关系。
- 检查 `#REF!`、`#VALUE!`、`#DIV/0!` 等常见 Excel 错误值。
- 问题至少区分“错误”和“警告”；存在错误时禁止导出。

#### 修复

- 检查、修复、导出是三个独立功能；用户必须先检查，修复后自动重新检查。
- 一次修复统一处理 12 个可见 Sheet 的序号、行高、布局、分页、打印区域和边框。
- 项目计划两张 Sheet 内容行标准行高为 `35.25`，其他 10 个 Sheet 为 `26.25`；只上调低于标准的内容行。
- 项目计划不补空白行；其他 10 个 Sheet 只使用已有空白行补到当前最后一页，不插入新行。
- 项目计划保留合并关系，不能使用格式刷调整行高；边框统一为黑色细实线。
- 修复前确认并备份源 Excel；修复失败时关闭 Excel 后用备份恢复，成功后保留备份。
- 前端按“检查”“修复”“导出PDF”“打开导出目录”横向显示流程，只有当前可执行步骤启用，并在每一步完成后提示下一步。

#### 候选 PDF

固定生成以下 11 类候选文件：

| 编号 | 文件标题 | 源 Sheet |
| --- | --- | --- |
| 1 | 生产计划 | `生产计划` |
| 2 | 项目计划 | `项目计划（锻压）`、`项目计划（海工）` |
| 3 | 技术准备计划 | `技术准备` |
| 4 | 工艺准备计划 | `工艺准备` |
| 5 | 毛坯、原材料配套计划 | `毛坯、原材料配套计划` |
| 6 | 采购件配套计划 | `采购配套计划` |
| 7 | 外协件配套计划 | `外协件配套计划` |
| 8 | 关键外协工序计划 | `关键外协工序计划` |
| 9 | 原材料下料计划 | `原材料下料` |
| 10 | 零件加工计划 | `零件加工` |
| 11 | 发运计划 | `产品发运` |

- 使用 Excel 原生 PDF 导出接口。
- 纸张为 A4 横向，列宽压缩为一页，高度按内容分页。
- 项目计划 PDF 固定按“项目计划（锻压）”后“项目计划（海工）”的顺序合并；其他 Sheet 使用已有空白行补足最后一页。
- 导出只读打开源 Excel，不执行修复或保存。
- 输出到时间戳预览目录，不自动替换正式版。

#### 验收标准

- 目录结构、文件数量或年月不符合要求时给出明确错误。
- 审查结果显示 Sheet、位置和问题说明。
- 存在错误时“生成候选 PDF”不可用。
- 检查后可以独立执行批量修复，修复前能够找到备份路径，修复后自动重新审查。
- 修复后的复查仍有错误时禁止导出，剩余问题由用户手动处理后重新检查。
- 审查通过后生成 11 份非空 PDF。
- 导出成功后源 Excel 不被导出流程修改；正式版文件不被自动修改或覆盖。
- 界面流程按“检查 → 修复 → 导出PDF → 打开导出目录”执行，按钮按顺序启用。

#### 限制与待开发

- 不自动验收页数、空白尾页或最终版式。
- 正式文件替换仍需要人工检查。
- Adobe PDFMaker 不是默认后端，需要真实样本验证兼容性。
- 依赖固定 Sheet 和表头结构。

### 5.5 生产会资料拆分

#### 目标

把只有一个 Sheet 的生产会资料按三个固定业务分段拆成三个 Sheet，并尽量保持原始 Excel 版式。

#### 输入要求

- 支持 `.xlsx`、`.xlsm` 和 `.xls`。
- 源文件必须只有一个 Sheet。
- 必须且只能出现以下三个标准标题，顺序固定：
  - `一、已发运项目`
  - `二、在制项目`
  - `三、预投项目`
- 第 2 行实际使用区域的最后一列必须是有效的开会日期。

#### 功能要求

- 复制完整源 Sheet，再生成“已发运项目、在制项目、预投项目”三个 Sheet。
- 删除其他分段和当前分段末尾的空白行。
- 保留公共表头、公式、合并单元格、隐藏行列、打印设置、冻结窗格、筛选、批注、超链接和其他主要版式。
- 检测红色或绿色背景填充；命中后对应列填充为白色，并清除条件格式填充属性，保留边框等其他属性。
- 重新计算并检查输出 Sheet 中的常见公式错误。
- 输出新 `.xlsx` 文件；输入 `.xlsm` 或 `.xls` 时不承诺保留 VBA 宏。

#### 验收标准

- 不支持的文件类型、非单 Sheet、标题缺失/重复/顺序错误时停止并提示。
- 无法读取第 2 行最后一列日期时停止并提示。
- 成功后输出文件存在且非空，包含三个固定名称和顺序的 Sheet。
- 源文件保持不变。
- 输出文件发现公式错误时，导出失败并提示。

#### 限制与待开发

- 强依赖三个固定标题和第 2 行日期布局。
- `.xlsm` 或 `.xls` 输入不保留 VBA 宏。
- 模板结构变化需要重新适配。

### 5.6 生产消息 Notion 入库

#### 当前实现（1.3.0）

- 已提供独立页面，支持兼容 Markdown 标题和常见 Unicode 符号的日期拆分、两种格式自动识别、多日独立卡片、逐条编辑预览和批量提交。
- 已支持塔筒日报库、每月累计库、每年累计库三类数据源的缓存选择和字段自动匹配保存；解析后先只读查询日报库，每条显示可写入、已存在或失败状态，已有记录通过黄色提示和红色“覆盖写入”显式确认。下料日报库仍可作为独立模块选配。
- 塔筒日报只接受六项当日数值：板材入库、型材入库、下料、焊接、产出套数、产出节数；0 值必须保留为 0。
- 塔筒日报写入前按业务日期查找对应的每月累计页和每年累计页，建立两条 Relation；累计值由 Notion Rollup 计算，程序不写入当月/全年累计。
- 已通过 Notion 只读方式核对当前三库的字段、Relation、Rollup、8 月前历史基线和 8 月已有记录；应用界面真实写入仍待人工验收。

#### 当前代码进度（2026-08-06）

| 代码范围 | 状态 | 说明 |
| --- | --- | --- |
| 页面、导航和首页入口 | 已完成 | 已接入 `production-message` 路由和生产消息页面。 |
| 消息标准化、日期拆分和两种格式解析 | 已完成 | 已覆盖下料、塔筒产线日报、单位提取、批量缺日期阻止和编辑回写。 |
| Notion 数据源绑定和字段映射 | 已完成 | 支持日报、每月累计、每年累计三库选择、字段检测、Relation 自动识别和配置保存。 |
| 日报检查、写入、重复覆盖和月/年 Relation | 代码已完成，待人工 UI 验收 | 已实现解析后只读查询、逐条状态、创建、更新、红色覆盖确认和两条汇总 Relation；不写月度计划或累计数值。 |
| 三库结构核对 | 已完成 | 已核对真实数据库字段、Relation、Rollup、8 月前基线和 8 月已有记录。 |
| 本地验证 | 已完成 | parser smoke 和真实日报样例检查通过，`publish/Debug` 发布通过，版本为 `1.4.0.0`。 |
| 真实 Notion 写入验收 | 待验证 | 需要在应用界面重新绑定三库并完成一次真实日报写入和 Rollup 核对。 |

#### 目标

接收生产人员提供的格式化消息，提取结构化字段，经过用户预览确认后写入 Notion。首期支持下料消息和塔筒产线日报两种格式，并为后续新增格式保留解析器扩展点。

#### 输入与前置条件

- 用户在多行文本框中粘贴一条或多段消息。
- 批量模式下每段消息必须以日期开头；日期前可有 Markdown 标题或外围符号，支持 `8.5`、`8月5日`、`2026-08-05` 及常见 Unicode 分隔符等形式。
- 单条消息没有日期时提示选择日期，默认当天；批量模式中无日期段落不能自动套用上一段日期。
- 同一批次允许混合两种格式；每段独立识别和处理。
- 用户在 Notion 中建立并授权以下三张塔筒数据源：塔筒产线日报库、塔筒产线每月累计库、塔筒产线每年累计库；下料日报库为可选独立模块数据源。
- 日报库必须有日期、六个当日数值字段，以及分别指向每月累计库和每年累计库的两个 Relation。
- 每月累计库和每年累计库必须预先建立对应月份/年份的唯一汇总页，并通过 Relation + Rollup 计算累计值。
- 程序复用 Notion 数据源发现、字段自动匹配建议和用户确认保存的配置；具体字段名以当前数据库结构为准。

#### 功能要求

- `MSG-01`：标准化空白、Markdown 标题、外围符号、全角标点、Unicode 日期分隔符和常见单位写法，再按合法日期边界拆分批量消息；不得把业务数值误识别为日期。
- `MSG-02`：自动识别消息类型；置信度不足时允许用户手动改选，不允许不确定内容直接写入。
- `MSG-03`：下料消息至少提取业务日期、工序/动作、班次、项目、材料、件数、重量、单位和原始消息；项目、材料等非核心信息允许为空。
- `MSG-04`：塔筒产线日报只提取板材入库、型材入库、下料、焊接、产出套数和产出节数六项当日数据；不要求产线字段。
- `MSG-05`：数字写入 Number 字段，重量、件数、套数和节数使用固定单位；支持单位别名，不进行未经确认的单位换算。
- `MSG-06`：日报中的 0 必须保留为数值 0；当月累计和全年累计不作为输入字段，由 Notion 汇总库的 Rollup 计算。
- `MSG-07`：每个日报数据库按业务日期只保留一条记录；允许提前录入未来日期，例如周五录入周末数据。
- `MSG-08`：解析完成后按业务日期对目标日报库执行只读检查，每条显示可写入、已存在或失败；同一业务日期已有记录时使用黄色提示，并将蓝色“确认写入”切换为红色“覆盖写入”，未确认前不得覆盖。
- `MSG-09`：解析结果必须先预览；多日结果按日期显示为独立卡片，使用原生日期选择器和目标数据库选择，只显示本次实际解析且会写入的字段。用户可以修改后提交；关键字段缺失、日期/数值/单位歧义时阻止写入。
- `MSG-10`：批量记录独立处理；正常记录先写入，重复记录单独确认，错误记录保留在待处理列表中，不阻塞其他记录。
- `MSG-11`：写入塔筒日报前，按业务日期查找对应的每月累计页和每年累计页；两页都存在且唯一时，日报建立两条 Relation。
- `MSG-12`：每月累计和每年累计由 Notion Rollup 根据日报 Relation 计算；程序不写入月度/年度累计数值。缺少对应汇总页、发现重复汇总页或 Relation 字段不匹配时，日报不得写入。
- `MSG-13`：新增消息格式由开发者增加独立解析器，Notion 数据库和字段映射由用户配置，不把格式判断写死在页面流程中。

#### 输出

- 下料消息写入下料日报库。
- 塔筒产线日报写入塔筒产线日报库。
- 塔筒产线日报关联对应的每月累计页和每年累计页。
- 保存原始消息、业务日期、消息类型和解析器版本，便于追溯。
- 程序不执行月度/年度汇总、差额或误差判断；这些由 Notion 的 Relation、Rollup、视图或公式完成。

#### 验收标准

- [ ] 单条下料消息可以识别、预览、编辑并写入正确数据库。
- [ ] 单条塔筒产线日报可以识别并写入一条结构化记录。
- [ ] 一次粘贴多段不同日期、不同格式的消息时，能逐段预览并路由到对应数据库。
- [ ] 多日解析结果按日期显示为独立卡片；空字段、累计字段等本次不写入内容不显示。
- [ ] 解析完成后只读查询目标日报库并逐条显示可写入、已存在或失败；真正写入前再次查询。
- [ ] 已有记录触发黄色提示和红色“覆盖写入”，无重复时显示蓝色“确认写入”。
- [ ] 单条无日期消息默认提示当天；批量无日期段落被阻止并明确提示。
- [ ] 同日期重复写入时能展示旧数据和新数据，用户确认后才覆盖。
- [ ] 关键字段缺失或数值、日期、单位有歧义时不能写入。
- [ ] 塔筒日报写入后，两个 Relation 分别指向正确的月份页和年份页。
- [ ] 每月累计库和每年累计库的 Rollup 随日报数据正确更新。
- [ ] 缺少或重复月份/年份汇总页时，日报不会写入并给出明确提示。
- [ ] 用户确认字段映射后，后续导入复用映射；字段缺失时明确提示。
- [x] 真实数据库建立后，已完成字段、Relation、Rollup 和 8 月前基线的只读核对。

#### 限制与待开发

- 首期只支持两种已确认格式；完全陌生的新格式需要增加解析器。
- 月度和年度汇总页需要用户预先建立且保持业务日期唯一，程序当前不会自动创建。
- Notion 写入不是服务端事务，批量写入可能出现部分成功。
- 模块只负责输入和写入，不在程序中分析计划完成情况或判断误差。

### 5.7 日报自动推送

#### 当前实现

- 只管理一套草稿和一套已启用模板；模板必须完成真实日期预览和钉钉测试发送后才能启用。
- 模板编辑采用 WebView2 承载的 Tiptap 结构化编辑器：固定文字可自由换行，业务日期和 Notion 字段以不可拆分的行内胶囊显示；字段胶囊正文按“日/月/年 · 字段”简写并限制宽度，悬停显示完整的“周期 · 数据库 · 字段”，内部保存数据源 ID、字段 ID、字段类型和格式。保存、预览和自动发送时继续导出兼容的 `prop("数据库 · 字段")` 与 `today("yyyy年M月d日")` 纯文本，旧版纯文本、普通日期标记和 Base64 标记加载时自动迁移。
- 插入字段时只选择已分好的日库、月库或年库和目标数据字段；程序复用生产消息模块的三库绑定自动识别周期，并从数据库结构中自动选择日期字段。日期属性分别按当天、整月、整年范围查询，不再要求用户重复选择数据口径和匹配字段。
- 每个来源在当前周期必须且只能匹配一条记录；缺失、重复、空值、失效字段、格式错误或网络失败均阻止正式日报。
- 钉钉机器人使用 HTTPS Webhook 和加签 Secret，发送纯文本并 @所有人；敏感配置使用当前 Windows 用户的 DPAPI 加密，页面只显示掩码。
- 钉钉连接状态保存在本地，打开页面不自动请求；点击“检测连接”会向群内发送一条明确的连接测试消息，测试发送也会刷新缓存状态。
- 发送时间可在页面自定义；点击“安装 / 更新任务”后，以当前用户创建每日 Windows 任务计划。时间或程序路径变化时页面提示重新同步。
- 自动任务使用 `ProductionAssistant.exe --send-daily-report` 无界面入口；同一业务日期和模板版本成功后不重复发送，网络失败最多尝试三次，失败时发送告警并写本地状态和脱敏日志。

#### 验收标准

- [ ] 空模板时页面保持正常宽度和左右布局，不因内容为空整体收缩或偏移。
- [ ] 日库按当天、月库按当前月份、年累计库按当前年份各匹配唯一记录并生成正确正文。
- [ ] 模板中的 Notion 字段显示为可整体选择、删除的行内胶囊；固定文字、多行换行、光标定位、复制粘贴和字段插入正常。
- [ ] 旧版 `today()` / `prop()` 纯文本和 Base64 草稿能迁移到结构化编辑器，保存后生成的兼容纯文本仍可被预览和无界面自动任务解析。
- [ ] 自定义发送时间保存后，任务计划显示相同时间；修改时间或发布路径后提示更新任务。
- [ ] 保存钉钉配置后显示掩码；未主动检测时不联网，检测和测试发送后显示带时间的缓存状态。
- [ ] 缺失、重复、空值、无效格式和网络错误不会发送半成品正式日报。
- [ ] 同一天重复启动无界面任务不会重复发送已成功的同版本日报。

#### 限制

- 本机在发送时段必须可用且当前 Windows 用户已登录；整日关机无法发送。
- 文本匹配字段必须采用 `yyyy-MM-dd`、`yyyy-MM` 或 `yyyy`，其他命名格式需要改用日期属性或调整 Notion 数据。
- 钉钉没有无消息的机器人健康检查接口，因此显式连接检测会产生一条群测试消息。
- 当前仅支持直接字段值和数字/日期格式，不支持模板公式、条件表达式或跨字段计算。

## 6. 产品级非功能要求

### 6.1 可用性

- 使用中文业务名称和明确的成功、警告、错误状态。
- 长任务显示进度或忙碌状态，并禁止重复操作。
- 常规设置不要求用户直接编辑配置文件。

### 6.2 数据安全

- 检查默认不修改源文件；修复显式备份并保存格式修正，导出只读且不修改源文件，正式版文件始终不自动修改。
- 自动修改动作必须提示修改范围和结果。
- Notion 令牌只能由保存它的同一 Windows 用户使用。

### 6.3 兼容性

- 目标运行环境为 Windows x64。
- 依赖 Excel、Notion 或固定模板的新模块，必须在模块需求中声明前置条件和失败边界。

## 7. 当前版本状态

### 已纳入当前版本

- 首页、统一导航和系统设置入口。
- 每日焊接数据模拟、人工调整和月度合计校验。
- Notion 数据源发现、字段绑定和月/周/日导入流程。
- 挂网计划 Excel 检查、可修复序号和格式、11 类候选 PDF 只读导出。
- 挂网计划前端横向流程和逐步启用按钮。
- 生产会单 Sheet Excel 的三段拆分。
- 生产消息 Notion 入库页面、两种格式解析、字段映射和重复覆盖保护。
- 日报自动推送页面、日/月/年取数、可读模板引用、钉钉加签发送和自定义任务计划。

### 已完成结构核对，仍需人工写入验收

- 生产消息当前三张 Notion 数据库的字段、Relation、Rollup 和 8 月前基线已完成只读核对。
- 仍需在应用界面验证真实日报写入、重复覆盖、两条 Relation 和 Rollup 更新。
- 挂网计划真实模板的序号、日期、打印分页和 PDF 版式。
- 生产会真实模板的合并、隐藏、条件格式、公式和宏兼容性。

### 待开发

- 每日焊接数据独立导出为 CSV 或 Excel。
- Notion 导入的事务性、回滚或可视化恢复工具。
- 候选 PDF 的自动页数、空白尾页和最终版式验收。
- 候选 PDF 自动替换正式版的受控流程。
- 更灵活的 Excel 模板识别和模板配置。
- 安装包、自动更新和自动化测试。

## 8. 新模块追加规则

当用户开启新模块时，按以下顺序追加：

1. 在第 3 节模块表新增一行。
2. 在第 5 节新增模块章节，写目标、输入、功能、输出、验收和限制。
3. 只把代码中已存在或已验证的行为写入当前实现；其他内容写入待开发。
4. 在 README 的功能列表增加一条简短说明。
5. 在 `HANDOFF.md` 的代码映射、验证清单和当前状态中同步更新。
6. 在本文档末尾追加变更记录。

### 8.1 新模块模板

```markdown
### 5.X 模块名称

#### 目标

说明要解决的业务问题和目标用户。

#### 输入

- 用户输入：
- 文件或数据源：
- 前置条件：

#### 功能要求

- MOD-01：
- MOD-02：

#### 输出

- 页面结果：
- 文件或外部系统结果：

#### 验收标准

- [ ] 验收项一
- [ ] 验收项二

#### 限制与待开发

- 待补充限制或待开发项
```

## 9. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-10 | 版本更新为 1.4.0：新增日报自动推送、Tiptap 字段胶囊模板、模块一级与功能二级导航，并增强生产消息解析、数据库检查和首页就绪看板。 |
| 2026-08-10 | 实现 WebView2 + Tiptap 结构化胶囊编辑器；按用户纠正后的信息架构，将左侧导航改为“业务模块一级菜单 + 真实功能二级页面”，首页的“本地工具 / Notion 工具”看板分类保持不变。结构化文档与兼容纯文本同时保存，桌面交互与视觉待人工验收。 |
| 2026-08-10 | 新增日报自动推送模块：组合多个 Notion 数据源，支持 `today()` / `prop()` 可读模板引用、日/月/年周期匹配、钉钉加签发送、缓存连接状态、自定义发送时间和 Windows 任务计划；本地构建、smoke 和 Debug publish 通过，桌面布局及真实链路待人工验收。 |
| 2026-08-09 | 首页重构为“本地工具 / Notion 工具”操作看板，四张模块卡片与侧栏共用导航；本地配置状态总览及整体页面方案已由用户确认。 |
| 2026-08-09 | 增强生产消息日期兼容和多日拆分；解析预览改为原生日期选择器、目标数据库和仅实际写入字段；新增解析后只读数据库检查、多日独立卡片、黄色已有记录提示及红色覆盖按钮。 |
| 2026-08-07 | 发布 1.3.0：新增生产消息 Notion 入库、解析预览、冲突后按需强制覆盖，并修复生产会资料拆分页面空白占位。 |
| 2026-08-05 | 实现 1.3.0 生产消息 Notion 入库模块：支持两种格式解析、多日期预览编辑、字段映射、重复覆盖保护和月度计划 Relation；真实 Notion 样本待验证。 |
| 2026-08-05 | 确认生产消息 Notion 入库模块方案：支持两种格式自动识别、多日期批量输入、逐条预览确认、重复覆盖提示和塔筒月度计划 Relation；汇总与误差判断交由 Notion。 |
| 2026-08-05 | 私有仓库曾发布 Production Assistant v1.2.0，并附 Windows x64 自包含压缩包。 |
| 2026-08-05 | 发布 1.2.0：完成挂网计划检查、修复、导出流程的横向前端布局和按步骤启用，并同步 Release 发布流程。 |
| 2026-08-04 | 按产品需求与开发交接的边界重组 PRD；保留功能、验收、限制和新模块模板，移出构建、Git 和接手操作信息。 |
